# 개인 연구 홈페이지 사양서 (SPEC)

이 문서는 프로젝트의 확정 사양이다.
이 문서와 어긋나는 구현을 하기 전에는 반드시 사용자에게 확인할 것.

---

## 1. 목적과 제약

**연구실 홈페이지가 아니다.** 연구자 개인이 자신의 연구 성과를 앞으로 축적해 나가기
위한 개인 사이트다. 따라서 구성원 소개, 소식 게시판 같은 요소는 두지 않는다.
정적 사이트로 GitHub Pages에 배포한다.

**핵심 제약 (변경 시 사용자 승인 필수)**

| 항목 | 결정 | 이유 |
|---|---|---|
| 빌드 도구 | **사용하지 않음** | 이동 중 휴대폰으로 갱신해야 함. 빌드가 있으면 폰에서 갱신 불가 |
| 프레임워크 | **사용하지 않음** (순수 HTML/CSS/JS) | 몇 달에 한 번 갱신하는 사이트. 환경 복구 부담 제거 |
| npm 의존성 | **없음** (package.json 자체를 만들지 않음) | 위와 동일 |
| 외부 CDN | 원칙적으로 사용하지 않음. 필요 시 사전 승인 | 오프라인·검열 환경에서의 깨짐 방지 |
| 설명 텍스트 | **HTML에 직접 작성** | 검색엔진 노출 확보 |
| 반복 데이터 | **JSON 분리 후 JS 렌더링** | 논문·수상 갱신 편의 |
| 언어 | **영어 단일** | 독자가 국제 학계다. 번역본 두 벌을 계속 맞추는 부담을 없앤다 |
| 배포 | GitHub 공개 저장소 + GitHub Pages | 무료, 저장소가 백업 겸함 |

---

## 2. 디렉토리 구조

```
research-site/
├── index.html              # 홈 — 소개 + 약력
├── research.html           # 연구 분야
├── publications.html       # 연구 실적 (4개 섹션 통합)
├── data/
│   ├── publications.json   # journal / conference / patent
│   └── awards.json         # 수상 및 영예
├── assets/
│   ├── css/style.css
│   ├── js/render.js        # JSON 로드 및 목록 렌더링
│   ├── js/layout.js        # 헤더/푸터 주입 (6항 참고)
│   ├── cv/cv.pdf           # CV 파일 (파일명 고정, 7항 참고)
│   └── img/
│       ├── profile.jpg
│       └── research/
├── SPEC.md
├── CLAUDE.md
└── README.md               # 데이터 갱신 방법 안내 (폰에서 볼 문서)
```

탭은 3개이며 루트의 HTML 3개와 1:1 대응한다.

---

## 3. 데이터 스키마

### 설계 원칙

휴대폰에서 GitHub 웹 편집기로 **직접 손으로 고칠 수 있어야 한다.**
따라서:

- 중첩 구조를 만들지 말 것 (배열 안의 평평한 객체까지만)
- 필드명은 축약하지 말 것 (`ttl` 금지, `title_en` 사용)
- 선택 필드도 빈 문자열 `""` 로 항상 존재시킬 것 (키 누락으로 인한 에러 방지)
- 정렬은 코드가 처리할 것 (사람이 순서를 맞추게 하지 말 것)
- `_en` 접미사는 영어 단일 사이트가 된 뒤에도 유지한다.
  나중에 한글을 다시 넣고 싶어지면 기존 항목을 건드리지 않고 `_ko` 를 덧붙이면 되기 때문이다

### 3.1 `data/publications.json`

```json
[
  {
    "id": "2026-jcp-01",
    "year": 2026,
    "type": "journal",
    "authors_en": "G. Hong, C. Kim, Y. Lee",
    "title_en": "Paper Title",
    "venue_en": "Journal Name 12(3), 45-67",
    "link": "https://doi.org/...",
    "highlight": false
  }
]
```

- `type`: `journal` | `conference` | `patent` — **이 세 가지 외의 값을 쓰지 말 것**
- `highlight`: `true` 이면 목록에서 강조 표시 (교신저자 논문, 대표 성과 등)
- `link`: 없으면 `""`
- 화면 정렬: `year` 내림차순, 같은 해 안에서는 **배열 역순**(뒤에 있는 항목이 위)
  → 폰에서 파일 끝에 객체를 추가하면 그 해의 맨 위에 표시된다 (8항 갱신 시나리오와 맞추기 위함)

**특허 표기 규칙**: 특허는 저자·게재지 구조를 그대로 재활용한다.

- `authors_en` → 발명자
- `venue_en` → `Reg. 10-1234567` 또는 `App. 10-2026-0001234` 형식
- `year` → 등록(또는 출원) 연도

### 3.2 `data/awards.json`

수상은 논문과 필드 구성이 달라 별도 파일로 분리한다.
(저자·게재지가 없고, 수여기관과 수상일이 있다)

```json
[
  {
    "id": "2026-best-paper",
    "date": "2026-05-01",
    "title_en": "Best Paper Award",
    "org_en": "Korean Society of OO",
    "link": ""
  }
]
```

- `date`: `YYYY-MM-DD` 고정. 일자를 모르면 해당 월의 01일로 기입
- 화면 정렬: `date` 내림차순

---

## 4. 페이지별 내용

| 페이지 | HTML 직접 작성 | JSON 렌더링 |
|---|---|---|
| index (Home) | 이름·소속, 한 줄 소개, 약력, 외부 프로필 링크, CV 링크, 이메일 | - |
| research | 연구 분야 설명 전문 | - |
| publications | 섹션 제목 4개만 | publications.json + awards.json |

### 4.1 Home 상세 구성

위에서부터 아래 순서로 배치한다.

1. 프로필 사진, 이름(영문 표기, 한글 병기), 현재 소속과 직위
2. 한 줄 소개 (한 문장)
3. **외부 프로필 링크** — Google Scholar, ORCID, ResearchGate, GitHub(선택), 이메일
   텍스트 링크 또는 단순 아이콘. 외부 아이콘 라이브러리를 불러오지 말 것 (1항 CDN 제약)
4. **CV 다운로드 링크** — `assets/cv/cv.pdf`
5. **약력** — 학력과 경력. 최신순. HTML에 직접 작성한다
   (변경 빈도가 낮고 검색 노출 대상이므로 JSON으로 빼지 않는다)
6. 연구 관심사 3~5줄 요약 (상세는 Research 탭으로 링크)

### 4.2 Publications 페이지 구성

한 페이지에 4개 섹션을 순서대로 둔다. 페이지 상단에 섹션 앵커 링크를 배치한다.

| 순서 | 섹션 | 출처 |
|---|---|---|
| 1 | Journal Articles | publications.json (`type: journal`) |
| 2 | Conference Presentations | publications.json (`type: conference`) |
| 3 | Patents | publications.json (`type: patent`) |
| 4 | Awards & Honors | awards.json |

- 앵커 id: `#journal`, `#conference`, `#patent`, `#awards`
- 각 섹션 안에서는 연도별로 묶어 표시한다
- **항목이 0건인 섹션은 섹션 자체를 숨긴다.** 빈 제목만 남으면 초라해 보인다

**향후 확장 메모**: 항목이 많아져 페이지가 길어지면 섹션별 페이지 분리를 검토한다.
데이터가 이미 `type` 으로 구분돼 있어 전환 비용은 작다. 단, 사용자 승인 없이 하지 말 것.

---

## 5. 렌더링 방식

- `fetch()` 로 JSON 로드 → DOM 생성
- 로드 실패 시 빈 화면이 아니라 **오류 메시지를 화면에 표시**할 것
  (폰에서 JSON 문법을 틀렸을 때 원인을 알 수 있어야 함)
- 화면에 쓰는 값은 `_en` 필드다
- 라이브러리 없이 순수 DOM API 사용

---

## 6. 헤더/푸터 중복 문제

HTML 파일이 3개이므로 메뉴 변경 시 3곳을 고쳐야 한다.
완화책으로 **헤더/푸터만 `assets/js/layout.js` 로 주입**한다.
내비게이션은 검색 노출 영향이 적어 이 방식이 허용된다.
단, 본문 텍스트는 절대 JS로 주입하지 말 것.

**메모**: 파일이 3개뿐이라 손으로 관리하는 것도 불가능하지 않다.
JS 주입을 걷어내고 각 HTML에 헤더를 직접 넣는 방식으로 바꾸고 싶다면 사용자와 상의할 것.

---

## 7. 배포

- GitHub **공개** 저장소 (Pages 무료 조건)
- 기본 브랜치 루트에서 Pages 서빙
- 커스텀 도메인은 추후 결정. 지금은 `username.github.io/repo` 형태로 진행
- CI/CD 설정은 만들지 않는다 (빌드가 없으므로 불필요)

**CV 파일 규칙**: 파일명을 `cv.pdf` 로 **고정**한다.
날짜를 파일명에 넣으면 갱신할 때마다 HTML의 링크를 함께 고쳐야 하고,
외부에 공유된 기존 링크가 깨진다.
최종 수정일은 HTML 본문에 텍스트로 표기한다 (예: `CV (updated 2026-08)`).

---

## 8. 갱신 시나리오 (설계가 지켜야 할 목표)

1. **논문 1편 추가** → 폰에서 GitHub 웹 편집기로 `publications.json` 에 객체 1개 추가 → 커밋. 끝.
2. **수상 1건 추가** → `awards.json` 에 객체 1개 추가. 끝.
3. **특허 등록** → `publications.json` 에 `type: "patent"` 로 추가. 끝.
4. **CV 갱신** → `assets/cv/cv.pdf` 를 같은 이름으로 교체. HTML 수정 불필요.
5. **레이아웃/디자인 수정** → PC의 Claude Code, 또는 이동 중이면 Claude Code on the web

1~4번이 AI 없이 손으로 가능해야 한다. 이것이 스키마 단순화와 파일명 고정의 이유다.

---

## 9. 사용자가 준비해야 할 원고

구현과 별개로 아래가 있어야 사이트가 완성된다.

- [ ] 이름, 소속, 직위 (영문)
- [ ] 한 줄 소개 (영문)
- [ ] 약력 — 학력 및 경력 (영문)
- [ ] 연구 분야 설명 (영문)
- [ ] 논문 / 학회 발표 / 특허 목록
- [ ] 수상 내역
- [ ] Google Scholar, ORCID, ResearchGate 프로필 URL
- [ ] CV PDF 파일
- [ ] 프로필 사진
- [ ] 공개용 이메일 주소

원고가 없는 구간은 **더미 텍스트를 채우지 말고 빈 상태로 두고 사용자에게 알릴 것.**
