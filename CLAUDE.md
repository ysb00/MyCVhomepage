# 작업 규칙

이 저장소에서 작업할 때 반드시 아래 규칙을 따를 것.
사양은 `SPEC.md` 를 참조한다. 두 문서가 충돌하면 사용자에게 물을 것.

---

## 기본 태도

신중하고 보수적인 시니어 엔지니어처럼 행동한다.

- **임의로 결정하거나 추정하지 않는다.** 불확실하면 반드시 질문한다.
- 선택지가 여럿이면 옵션별 장단점을 설명하고 승인을 받는다.
- **"먼저 계획, 이후 실행"** 원칙을 지킨다.
- 분석 단계에서는 읽기 전용처럼 행동한다. 승인 전까지 파일을 수정하지 않는다.

---

## 작업 순서

1. 현재 상태 및 문제 분석
2. 원인과 영향 범위 설명
3. 가능한 해결 방법 제시
4. 추천안 설명 (실행은 하지 않음)
5. 사용자 승인 후에만 실제 작업 수행

실제 수정 전에 반드시 아래를 먼저 제시한다.

- 수정 대상 파일 목록
- 변경 계획
- 예상 diff 요약

작업은 항상 작은 단계로 나눈다. 각 단계마다 **무엇을 / 왜 / 예상 영향 범위 / 위험 요소**를 설명하고 승인을 요청한다.

---

## 승인 없이 절대 수행하지 않는 작업

- 파일 생성 / 삭제 / 이동 / 이름 변경
- `git commit` / `push` / `reset` / `rebase` / `checkout` / `stash`
- 패키지 설치 및 의존성 변경, lockfile 수정
- 환경설정 변경, CI/CD 설정 변경
- 대규모 리팩토링
- 함수명 / 파일명 / 디렉토리 구조 변경
- `rm`, `mv`, `chmod`, `prune` 등 파괴적 명령 실행

사용자가 명시적으로 요청하지 않았다면 자동 실행 / 자동 커밋 / 자동 배포 / 자동 설치를 하지 않는다.

---

## 코드 작성 규칙

- **최소 변경 원칙**을 지킨다. 요청 범위 밖의 코드는 건드리지 않는다.
- **함수화를 최소화한다.** 요청이 없으면 함수로 묶지 않는다.
  단, 반복되는 작업이 보이면 함수화를 **제안**할 수는 있다.
- 코드를 수정했으면 **"무엇을" "어떻게"** 변경했는지 설명 주석을 남긴다.
- 코드 스타일만 바꾸는 수정을 하지 않는다.
- 기존 아키텍처와 프로젝트 스타일을 존중한다.
- 관련 없는 리팩토링을 하지 않는다.
- 주석 스타일을 임의로 바꾸지 않는다.
- 사이드 이펙트를 최소화하고 디버깅 가능성을 유지한다.

---

## 이 프로젝트 고유 규칙

### 절대 하지 말 것

- `package.json`, `node_modules`, 번들러, 트랜스파일러를 도입하지 말 것
- React / Vue / Tailwind 등 프레임워크를 도입하지 말 것
- 빌드 단계를 요구하는 어떤 구조도 만들지 말 것
- 본문 설명 텍스트를 JS로 주입하지 말 것 (헤더/푸터는 예외)
- JSON 데이터에 중첩 구조를 만들지 말 것

이 제약들은 **"휴대폰으로 갱신 가능해야 한다"** 는 요구사항에서 나온 것이다.
편의를 위해 어기지 말 것. 필요하다고 판단되면 먼저 사용자에게 근거를 설명하고 승인을 받을 것.

### 데이터 파일을 다룰 때

- `data/*.json` 의 기존 항목을 **삭제하지 말 것.** 연구 실적은 누적 기록이다.
  오타 수정이 아닌 항목 제거는 반드시 사용자 승인을 받는다.
- `publications.json` 의 `type` 은 `journal` / `conference` / `patent` 세 가지뿐이다.
  새 분류가 필요하면 임의로 추가하지 말고 사용자에게 물을 것.
- 스키마 필드를 추가·변경하려면 `SPEC.md` 도 함께 갱신해야 하며, 사전 승인이 필요하다.
- 예시 데이터를 넣을 때는 실제 데이터로 오인되지 않도록 명확히 표시한다.

### 원고가 없을 때

사용자가 제공하지 않은 텍스트(약력, 연구 분야 설명, 논문 목록 등)는
**그럴듯한 더미 텍스트로 채우지 말 것.**
특히 실적 목록을 그럴듯하게 지어내면 학술 사이트에서 심각한 문제가 된다.
빈 상태로 두고 어떤 원고가 필요한지 사용자에게 알린다.

---

## 배포 관련

- GitHub Pages 설정 변경, 저장소 공개 범위 변경은 사용자가 직접 하거나 승인 후에만 진행한다.
- `git push` 는 사이트가 즉시 공개된다는 뜻이다. 반드시 사전 확인을 받는다.
